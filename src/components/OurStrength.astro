---
import { siteConfig } from "@/config/site.config";
---

<section id="our-strength" class="border-t border-slate-800 bg-[#0d0d0d] py-20 md:py-28">
  <div class="mx-auto max-w-6xl px-6">
    <h2 class="text-2xl font-bold text-white md:text-3xl">
      {siteConfig.ourStrength.title}
    </h2>
    <p class="mt-3 text-slate-400">
      {siteConfig.ourStrength.subtitle}
    </p>

    <div class="mt-14 grid gap-12 lg:grid-cols-2 lg:items-center">
      <div class="overflow-hidden rounded-2xl bg-slate-800">
        <div class="aspect-[4/3] bg-gradient-to-br from-slate-700 to-slate-800 flex items-center justify-center text-slate-500">
          <span class="text-sm">이미지 영역</span>
        </div>
      </div>
      <div class="space-y-8">
        {
          siteConfig.ourStrength.items.map((item) => (
            <div>
              <span class="text-sm font-bold text-red-500">{item.number}</span>
              <h3 class="mt-1 text-lg font-bold text-white">{item.title}</h3>
              <p class="mt-2 text-sm text-slate-400">{item.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
